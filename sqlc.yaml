version: "2"
sql:
  - engine: "postgresql"
    queries: "/db/queries"
    schema: "/db/schema.sql"
    gen:
      go:
        package: "db"
        out: "db"
        sql_package: "pgx/v5"
        overrides:
          - db_type: "uuid"
            go_type: "github.com/google/uuid.UUID"
          - db_type: "JSONB"
            go_type: "json.RawMessage"